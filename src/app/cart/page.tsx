// sections
import { CartView } from 'src/sections/cart/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Foodo: Your Cart',
};

export default function HomePage() {
  return <CartView />;
}
