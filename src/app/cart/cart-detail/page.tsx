// sections
import { CartDetailsView } from 'src/sections/cart/view';

// ----------------------------------------------------------------------

export const metadata = {
  title: 'Foodo: Cart Detail',
};

export default function HomePage() {
  return <CartDetailsView />;
}
